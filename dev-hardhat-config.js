const DEV_NETWORK_NAME = ["hardhat", "localhost"]

const MOCK_INITIAL_ANSWER = 186000000000
const MOCK_DECIMAL = 8

module.exports = {
    DEV_NETWORK_NAME,
    
    MOCK_INITIAL_ANSWER,
    MOCK_DECIMAL
}